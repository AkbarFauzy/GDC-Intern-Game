using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class GameOver : MonoBehaviour
{
 /*   public Text GO;
    // Start is called before the first frame update
    void Start()
    {
        bool isAllDead = true;
        for (int i = 0; i < 4; i++)
        {
            if (GameManager.Instance.playerRank[i].Hp != 0)
            {
                isAllDead = false;
                break;
            }
        }

        if (isAllDead)
        {
            GO.enabled = true;

            return;
        }
        else
        {
            GO.enabled = false;
        }
    }*/

}
